# PTRun Client Component(s)

JS and Phaser-based component(s) providing client for PTRun.

## Linting

To lint files using ESLint, run:

```shell
npm run lint
```

To apply all available fixes, run:

```shell
npm run lint-fix
```
