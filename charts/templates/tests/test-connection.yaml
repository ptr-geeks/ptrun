apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ptrun.fullname" . }}-test-connection"
  labels:
    {{- include "ptrun.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ptrun.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
